import { SlicePlaceholder } from "../slice-placeholder";

// M6M §1 — tab slot 5. The real screen is M-7 (§4.7), a later slice.
export default function MobileFieldPage() {
  return <SlicePlaceholder screen="M-7 · Field" title="Field" />;
}
