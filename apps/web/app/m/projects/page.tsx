import { SlicePlaceholder } from "../slice-placeholder";

// M6M §1 — tab slot 1. The real screen is M-2 (§4.2), a later slice.
export default function MobileProjectsPage() {
  return <SlicePlaceholder screen="M-2 · Projects" title="Projects" />;
}
